CREATE TABLE user_site(
    user_id int,
    site_title varchar(255),
    PRIMARY KEY(user_id, site_title),
    FOREIGN KEY(user_id) REFERENCES users(id), 
    FOREIGN KEY(site_title) REFERENCES site(title)
);